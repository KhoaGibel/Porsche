import { AccountRequest, Account, AccountsRequest, Paginated, AccountsBulkRequest, AccountStatsRequest, AccountStats, AccountFeedsStatsRequest, AccountFeedsStats, AccountGraphsStatsRequest, AccountGraphsFollowStats, AccountsAvailableRequest, AccountAvailable, AccountsBlockedRequest, AccountBlocked, SetAccountMetadataRequest, SetAccountMetadataResult, CreateAccountWithUsernameRequest, CreateAccountWithUsernameResult, CreateAccountRequest, CreateAccountResult, EnableSignlessResult, RemoveSignlessResult, MuteRequest, UnmuteRequest, ReportAccountRequest, BlockRequest, BlockResult, UnblockRequest, UnblockResult, RecommendAccountRequest, UndoRecommendAccountRequest, UpdateAccountFollowRulesRequest, UpdateAccountFollowRulesResult, AccountManagersRequest, AccountManager, AddAccountManagerRequest, AddAccountManagerResult, RemoveAccountManagerRequest, RemoveAccountManagerResult, UpdateAccountManagerRequest, UpdateAccountManagerResult, HideManagedAccountRequest, UnhideManagedAccountRequest, ConfigurePostActionRequest, ConfigurePostActionResult, EnablePostActionRequest, EnablePostActionResult, DisablePostActionRequest, DisablePostActionResult, ExecutePostActionRequest, ExecutePostActionResult, ConfigureAccountActionRequest, ConfigureAccountActionResult, EnableAccountActionRequest, EnableAccountActionResult, DisableAccountActionRequest, DisableAccountActionResult, ExecuteAccountActionRequest, ExecuteAccountActionResult, AddAdminsRequest, AddAdminsResult, RemoveAdminsRequest, RemoveAdminsResult, AdminsForRequest, Admin, AppRequest, App, AppsRequest, AppGroupsRequest, Group, AppFeedsRequest, AppFeed, AppSignersRequest, AppSigner, AppUsersRequest, AppUser, CreateAppRequest, CreateAppResult, AddAppFeedsRequest, AddAppFeedsResult, AddAppGroupsRequest, AddAppGroupsResult, AddAppSignersRequest, AddAppSignersResult, RemoveAppFeedsRequest, RemoveAppFeedsResult, RemoveAppGroupsRequest, RemoveAppGroupsResult, RemoveAppSignersRequest, RemoveAppSignersResult, SetAppGraphRequest, SetAppGraphResult, SetDefaultAppFeedRequest, SetDefaultAppFeedResult, SetAppMetadataRequest, SetAppMetadataResult, SetAppVerificationRequest, SetAppVerificationResult, SetAppSponsorshipRequest, SetAppSponsorshipResult, SetAppTreasuryRequest, SetAppTreasuryResult, SetAppUsernameNamespaceRequest, SetAppUsernameNamespaceResult, AddAppAuthorizationEndpointRequest, RemoveAppAuthorizationEndpointRequest, AppServerApiKeyRequest, GenerateNewAppServerApiKeyRequest, ServerAPIKey, AuthenticatedSession, RevokeAuthenticationRequest, RefreshRequest, RefreshResult, AuthenticatedSessionsRequest, RolloverRefreshRequest, SwitchAccountRequest, SwitchAccountResult, MeResult, LastLoggedInAccountRequest, CreateFeedRequest, CreateFeedResult, SetFeedMetadataRequest, SetFeedMetadataResult, FeedRequest, Feed, FeedsRequest, UpdateFeedRulesRequest, UpdateFeedRulesResult, CreateFollowRequest, FollowResult, CreateUnfollowRequest, UnfollowResult, FollowersRequest, Follower, FollowingRequest, Following, FollowersYouKnowRequest, FollowStatusRequest, FollowStatusResult, CreateFrameTypedDataRequest, CreateFrameEip712TypedDataFragment, SignFrameActionRequest, FrameLensManagerSignatureResultFragment, VerifyFrameSignatureRequest, FrameVerifySignatureResult, AccountBalancesRequest, AnyAccountBalance, BalancesBulkRequest, AnyBalance, WithdrawRequest, WithdrawResult, DepositRequest, DepositResult, WrapTokensRequest, WrapTokensResult, UnwrapTokensRequest, UnwrapTokensResult, CreateGraphRequest, CreateGraphResult, SetGraphMetadataRequest, SetGraphMetadataResult, GraphRequest, Graph, GraphsRequest, UpdateGraphRulesRequest, UpdateGraphRulesResult, CreateGroupRequest, CreateGroupResult, SetGroupMetadataRequest, SetGroupMetadataResult, JoinGroupRequest, JoinGroupResult, LeaveGroupRequest, LeaveGroupResult, GroupRequest, GroupsRequest, GroupMembersRequest, GroupMember, GroupStatsRequest, GroupStatsResponse, GroupBannedAccountsRequest, GroupBannedAccount, GroupMembershipRequestsRequest, GroupMembershipRequest, UpdateGroupRulesRequest, UpdateGroupRulesResult, ApproveGroupMembershipRequest, ApproveGroupMembershipResult, RemoveGroupMembersRequest, RemoveGroupMembersResult, RequestGroupMembershipRequest, RequestGroupMembershipResult, CancelGroupMembershipRequestRequest, CancelGroupMembershipRequestResult, RejectGroupMembershipRequest, RejectGroupMembershipResult, BanGroupAccountsRequest, BanGroupAccountsResult, UnbanGroupAccountsRequest, UnbanGroupAccountsResult, NativeAmount, Erc20Amount, RefreshMetadataStatusRequest, RefreshMetadataStatusResult, RefreshMetadataRequest, RefreshMetadataResult, AccessControlRequest, AccessControlResult, TokenDistributionsRequest, TokenDistribution, AccountRecommendationsRequest, PostsForYouRequest, PostForYou, PostsExploreRequest, Post, DismissRecommendedAccountsRequest, PostNotInterestedRequest, CreateUsernameNamespaceRequest, CreateUsernameNamespaceResult, SetNamespaceMetadataRequest, SetNamespaceMetadataResult, NamespaceRequest, UsernameNamespace, NamespacesRequest, UpdateNamespaceRulesRequest, UpdateNamespaceRulesResult, UpdateReservedUsernamesRequest, UpdateReservedUsernamesResult, NamespaceReservedUsernamesRequest, UsernameReserved, NotificationsRequest, Notification, CreatePostRequest, PostResult, CreateRepostRequest, EditPostRequest, DeletePostRequest, DeletePostResult, AddReactionRequest, AddReactionResult, UndoReactionRequest, UndoReactionResult, BookmarkPostRequest, UndoBookmarkPostRequest, HideReplyRequest, UnhideReplyRequest, ReportPostRequest, UpdatePostRulesRequest, UpdatePostRulesResult, PostRequest, AnyPost, PostsRequest, PostActionContractsRequest, PostActionContract, PostReactionsRequest, AccountPostReaction, PostBookmarksRequest, PostReferencesRequest, PostTagsRequest, PostTag, PostReactionStatusRequest, PostReactionStatus, WhoReferencedPostRequest, WhoExecutedActionOnPostRequest, PostExecutedActions, WhoExecutedActionOnAccountRequest, AccountExecutedActions, PostEditsRequest, PostEdit, GetSnsSubscriptionsRequest, SnsSubscription, CreateSnsSubscriptionRequest, DeleteSnsSubscriptionRequest, SponsorshipRequest, Sponsorship, SponsorshipsRequest, SponsorshipSignersRequest, SponsorshipSigner, SponsorshipGrantsRequest, SponsorshipGrant, SponsorshipLimitExclusionsRequest, SponsorshipLimitsExempt, CreateSponsorshipRequest, CreateSponsorshipResult, SetSponsorshipMetadataRequest, SetSponsorshipMetadataResult, UpdateSponsorshipLimitsRequest, UpdateSponsorshipLimitsResult, UpdateSponsorshipExclusionListRequest, UpdateSponsorshipExclusionListResult, UpdateSponsorshipSignersRequest, UpdateSponsorshipSignersResult, PausingRequest, PausingResult, TimelineRequest, TimelineItem, TimelineHighlightsRequest, TransactionStatusRequest, TransactionStatusResult, PrepareSignerErc20ApprovalRequest, PrepareSignerErc20ApprovalResult, TransferPrimitiveOwnershipRequest, TransferPrimitiveOwnershipResult, CanCreateUsernameRequest, CanCreateUsernameResult, CreateUsernameRequest, CreateUsernameResult, AssignUsernameToAccountRequest, AssignUsernameToAccountResult, UnassignUsernameFromAccountRequest, UnassignUsernameToAccountResult, UsernameRequest, Username, UsernamesRequest } from '@lens-protocol/graphql';
import { ResultAsync, Result, EvmAddress, UUID } from '@lens-protocol/types';
import { c as AnyClient, f as UnexpectedError, S as SessionClient, U as UnauthenticatedError, M as MetadataIndexingError } from '../clients-DlizIY8p.js';
import { InvariantError } from '@lens-chain/storage-client';
import '@lens-protocol/storage';
import '@urql/core';
import '@lens-protocol/env';

/**
 * Fetch an Account.
 *
 * Using a {@link SessionClient} will yield {@link Account#operations} specific to the authenticated Account.
 *
 * ```ts
 * const result = await fetchAccount(anyClient, {
 *   address?: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 * });
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The Account query request.
 * @returns The Account or `null` if it does not exist.
 */
declare function fetchAccount(client: AnyClient, request: AccountRequest): ResultAsync<Account | null, UnexpectedError>;
/**
 * Fetch an Accounts.
 *
 * Using a {@link SessionClient} will yield {@link Account#operations} specific to the authenticated Account.
 *
 * ```ts
 * const result = await fetchAccounts(anyClient);
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The query request.
 * @returns The list of accounts.
 */
declare function fetchAccounts(client: AnyClient, request?: AccountsRequest): ResultAsync<Paginated<Account>, UnexpectedError>;
/**
 * Fetch an Accounts Bulk.
 *
 * ```ts
 * const result = await fetchAccountsBulk(anyClient, {
 *  addresses: [evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5')],
 * });
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The query request.
 * @returns The list of accounts.
 */
declare function fetchAccountsBulk(client: AnyClient, request?: AccountsBulkRequest): ResultAsync<Account[], UnexpectedError>;
/**
 * Fetch an Account Stats.
 *
 * ```ts
 * const result = await fetchAccountStats(anyClient, {
 *   account: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 * });
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The query request.
 * @returns The stats for the account or `null` if it does not exist.
 */
declare function fetchAccountStats(client: AnyClient, request: AccountStatsRequest): ResultAsync<AccountStats | null, UnexpectedError>;
/**
 * Fetch an Account Feed Stats.
 *
 * ```ts
 * const result = await fetchAccountFeedStats(anyClient, {
 *   account: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 * });
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The query request.
 * @returns The feed stats for the account or `null` if it does not exist.
 */
declare function fetchAccountFeedStats(client: AnyClient, request: AccountFeedsStatsRequest): ResultAsync<AccountFeedsStats | null, UnexpectedError>;
/**
 * Fetch an Account Graph Stats.
 *
 * ```ts
 * const result = await fetchAccountGraphStats(anyClient, {
 *   account: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 * });
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The query request.
 * @returns The follow stats for the account or `null` if it does not exist.
 */
declare function fetchAccountGraphStats(client: AnyClient, request: AccountGraphsStatsRequest): ResultAsync<AccountGraphsFollowStats | null, UnexpectedError>;
/**
 * Fetch Accounts Available.
 *
 * ```ts
 * const result = await fetchAccountsAvailable(anyClient, {
 *   managedBy: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 * });
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The query request.
 * @returns The list of available accounts.
 */
declare function fetchAccountsAvailable(client: AnyClient, request: AccountsAvailableRequest): ResultAsync<Paginated<AccountAvailable>, UnexpectedError>;
/**
 * Fetch Blocked Accounts.
 *
 * ```ts
 * const result = await fetchAccountsBlocked(sessionClient);
 * ```
 *
 * @param client - The session client for the authenticated Account.
 * @param request - The query request.
 * @returns The list of blocked accounts.
 */
declare function fetchAccountsBlocked(client: SessionClient, request?: AccountsBlockedRequest): ResultAsync<Paginated<AccountBlocked>, UnexpectedError>;
/**
 * Set Account metadata for the authenticated Account.
 *
 * ```ts
 * const result = await setAccountMetadata(sessionClient, {
 *   metadataURI: uri('ar://abc123def456gh…'),
 * });
 * ```
 *
 * @param client - The session client for the authenticated Account.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function setAccountMetadata(client: SessionClient, request: SetAccountMetadataRequest): ResultAsync<SetAccountMetadataResult, UnexpectedError | UnauthenticatedError>;
/**
 * Create an account with a given username.
 *
 * ```ts
 * const result = await createAccountWithUsername(sessionClient, {
 *   username: {
 *      localname: 'wagmi'
 *   },
 *   metadataUri: uri('lens://bafybxiky5jf…'),
 * });
 * ```
 *
 * @param client - The session client for the authenticated Account.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function createAccountWithUsername(client: SessionClient, request: CreateAccountWithUsernameRequest): ResultAsync<CreateAccountWithUsernameResult, UnexpectedError | UnauthenticatedError>;
/**
 * Create an account without username.
 *
 * @alpha This is an alpha API and may be subject to breaking changes.
 *
 * ```ts
 * const result = await createAccount(sessionClient, {
 *   metadataUri: uri('lens://bafybxiky5jf…'),
 * });
 * ```
 *
 * @param client - The session client for the authenticated Account.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function createAccount(client: SessionClient, request: CreateAccountRequest): ResultAsync<CreateAccountResult, UnexpectedError | UnauthenticatedError>;
/**
 * Get transaction to enable signless.
 *
 * ```ts
 * const result = await enableSignless(sessionClient);
 * ```
 *
 * @param client - The session client for the authenticated Account.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function enableSignless(client: SessionClient): ResultAsync<EnableSignlessResult, UnexpectedError | UnauthenticatedError>;
/**
 * Get transaction to remove signless.
 *
 * ```ts
 * const result = await removeSignless(sessionClient);
 * ```
 *
 * @param client - The session client for the authenticated Account.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function removeSignless(client: SessionClient): ResultAsync<RemoveSignlessResult, UnexpectedError | UnauthenticatedError>;
/**
 * Mute an account.
 *
 * ```ts
 * const result = await muteAccount(sessionClient, {
 *   account: evmAddress("0xe5439696f4057aF073c0FB2dc6e5e755392922e1"),
 * });
 * ```
 *
 * @param client - The session client for the authenticated Account.
 * @param request - The mutation request.
 * @returns void.
 */
declare function muteAccount(client: SessionClient, request: MuteRequest): ResultAsync<void, UnexpectedError | UnauthenticatedError>;
/**
 * Unmute an account.
 *
 * ```ts
 * const result = await unmuteAccount(sessionClient, {
 *   account: evmAddress("0xe5439696f4057aF073c0FB2dc6e5e755392922e1"),
 * });
 * ```
 *
 * @param client - The session client for the authenticated Account.
 * @param request - The mutation request.
 * @returns void.
 */
declare function unmuteAccount(client: SessionClient, request: UnmuteRequest): ResultAsync<void, UnexpectedError | UnauthenticatedError>;
/**
 * Report an account.
 *
 * ```ts
 * const result = await reportAccount(sessionClient, {
 *   account: evmAddress("0xe5439696f4057aF073c0FB2dc6e5e755392922e1"),
 *   reason: AccountReportReason.RepetitiveSpam,
 * });
 * ```
 *
 * @param client - The session client for the authenticated Account.
 * @param request - The mutation request.
 * @returns void.
 */
declare function reportAccount(client: SessionClient, request: ReportAccountRequest): ResultAsync<void, UnexpectedError | UnauthenticatedError>;
/**
 * Block an account.
 *
 * ```ts
 * const result = await blockAccount(sessionClient, {
 *   account: evmAddress("0xe5439696f4057aF073c0FB2dc6e5e755392922e1"),
 * });
 *
 * @param client - The session client for the authenticated Account.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function blockAccount(client: SessionClient, request: BlockRequest): ResultAsync<BlockResult, UnexpectedError | UnauthenticatedError>;
/**
 * Unblock an account.
 *
 * ```ts
 * const result = await unblockAccount(sessionClient, {
 *   account: evmAddress("0xe5439696f4057aF073c0FB2dc6e5e755392922e1"),
 * });
 *
 * @param client - The session client for the authenticated Account.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function unblockAccount(client: SessionClient, request: UnblockRequest): ResultAsync<UnblockResult, UnauthenticatedError | UnexpectedError>;
/**
 * Recommend an account.
 *
 * ```ts
 * const result = await recommendAccount(sessionClient, {
 *   account: evmAddress("0xe5439696f4057aF073c0FB2dc6e5e755392922e1"),
 * });
 * ```
 *
 * @param client - The session client for the authenticated Account.
 * @param request - The mutation request.
 * @returns void.
 */
declare function recommendAccount(client: SessionClient, request: RecommendAccountRequest): ResultAsync<void, UnexpectedError | UnauthenticatedError>;
/**
 * Undo recommendation of an account.
 *
 * ```ts
 * const result = await undoRecommendAccount(sessionClient, {
 *   account: evmAddress("0xe5439696f4057aF073c0FB2dc6e5e755392922e1"),
 * });
 * ```
 *
 * @param client - The session client for the authenticated Account.
 * @param request - The mutation request.
 * @returns void.
 */
declare function undoRecommendAccount(client: SessionClient, request: UndoRecommendAccountRequest): ResultAsync<void, UnexpectedError | UnauthenticatedError>;
/**
 * Update account follow rules
 *
 * ```ts
 * const result = await updateAccountFollowRules(sessionClient, {
 *   toRemove: ['1234'],
 * });
 * ```
 *
 * @param client - The session client for the authenticated Account.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function updateAccountFollowRules(client: SessionClient, request: UpdateAccountFollowRulesRequest): ResultAsync<UpdateAccountFollowRulesResult, UnauthenticatedError | UnexpectedError>;

/**
 * Fetch Account Managers.
 *
 * ```ts
 * const result = await fetchAccountManagers(sessionClient);
 * ```
 *
 * @param client - Lens SessionClient.
 * @param request - The query request.
 * @returns List of Account Managers.
 */
declare function fetchAccountManagers(client: SessionClient, request?: AccountManagersRequest): ResultAsync<Paginated<AccountManager>, UnexpectedError | UnauthenticatedError>;
/**
 * Add an account manager to the authenticated account.
 *
 * ```ts
 * const result = await addAccountManager(sessionClient, {
 *   address: evmAddress("0xe5439696f4057aF073c0FB2dc6e5e755392922e1"),
 * });
 * ```
 *
 * @param client - Lens SessionClient.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function addAccountManager(client: SessionClient, request: AddAccountManagerRequest): ResultAsync<AddAccountManagerResult, UnexpectedError | UnauthenticatedError>;
/**
 * Remove manager from the authenticated account.
 *
 * ```ts
 * const result = await removeAccountManager(sessionClient, {
 *   manager: evmAddress("0xe5439696f4057aF073c0FB2dc6e5e755392922e1");
 * });
 * ```
 *
 * @param client - Lens SessionClient.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function removeAccountManager(client: SessionClient, request: RemoveAccountManagerRequest): ResultAsync<RemoveAccountManagerResult, UnexpectedError | UnauthenticatedError>;
/**
 * Update permissions for an account manager.
 *
 * ```ts
 * const result = await updateAccountManager(sessionClient, {
 *   permissions: {
 *       canSetMetadataUri: true;
 *       canTransferNative: false;
 *       canTransferTokens: true;
 *       canExecuteTransactions: false;
 *   },
 *   manager: evmAddress("0xe5439696f4057aF073c0FB2dc6e5e755392922e1");
 * });
 * ```
 *
 * @param client - Lens SessionClient.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function updateAccountManager(client: SessionClient, request: UpdateAccountManagerRequest): ResultAsync<UpdateAccountManagerResult, UnexpectedError | UnauthenticatedError>;
/**
 * Hide a managed account.
 *
 * ```ts
 * const result = await muteAccount(sessionClient, {
 *   account: evmAddress("0xe5439696f4057aF073c0FB2dc6e5e755392922e1");
 * });
 * ```
 *
 * @param client - The session client for the authenticated Account.
 * @param request - The mutation request.
 * @returns void.
 */
declare function hideManagedAccount(client: SessionClient, request: HideManagedAccountRequest): ResultAsync<void, UnexpectedError | UnauthenticatedError>;
/**
 * Unhide a managed account.
 *
 * ```ts
 * const result = await unhideManagedAccount(sessionClient, {
 *   account: evmAddress("0xe5439696f4057aF073c0FB2dc6e5e755392922e1");
 * });
 * ```
 *
 * @param client - The session client for the authenticated Account.
 * @param request - The mutation request.
 * @returns void.
 */
declare function unhideManagedAccount(client: SessionClient, request: UnhideManagedAccountRequest): ResultAsync<void, UnexpectedError | UnauthenticatedError>;

/**
 * Configure post action.
 *
 * ```ts
 * const result = await configurePostAction(sessionClient, {
 *   post: postId('1234…'),
 *   params: {
 *     simpleCollect: {
 *       payToCollect: {
 *         amount: {
 *           value: '100',
 *           currency: evmAddress('0x5678…'),
 *         },
 *       },
 *     },
 *   },
 * });
 * ```
 *
 * @param client - The session client for the authenticated Account.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function configurePostAction(client: SessionClient, request: ConfigurePostActionRequest): ResultAsync<ConfigurePostActionResult, UnexpectedError | UnauthenticatedError>;
/**
 * Enable post action.
 *
 * ```ts
 * const result = await enablePostAction(sessionClient, {
 *   post: postId('1234…'),
 *   action: { simpleCollect: true }
 * });
 * ```
 *
 * @param client - The session client for the authenticated Account.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function enablePostAction(client: SessionClient, request: EnablePostActionRequest): ResultAsync<EnablePostActionResult, UnexpectedError | UnauthenticatedError>;
/**
 * Disable post action.
 *
 * ```ts
 * const result = await disablePostAction(sessionClient, {
 *   post: postId('1234…'),
 *   action: { simpleCollect: true }
 * });
 * ```
 *
 * @param client - The session client for the authenticated Account.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function disablePostAction(client: SessionClient, request: DisablePostActionRequest): ResultAsync<DisablePostActionResult, UnexpectedError | UnauthenticatedError>;
/**
 * Execute post action.
 *
 * ```ts
 * const result = await executePostAction(sessionClient, {
 *   post: postId('1234…'),
 *   action: {
 *     simpleCollect: {
 *       selected: true,
 *     }
 *   }
 * });
 * ```
 *
 * @param client - The session client for the authenticated Account.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function executePostAction(client: SessionClient, request: ExecutePostActionRequest): ResultAsync<ExecutePostActionResult, UnexpectedError | UnauthenticatedError>;
/**
 * Configure account action, only available for configure custom actions.
 * By default the tipping action is configured for all accounts.
 * Any user can tip any other user and any token.
 *
 * ```ts
 * const result = await configureAccountAction(sessionClient, {
 *   action: {
 *     unknown: {
 *       address: evmAddress('0x1234…'),
 *       params: [{
 *         key: 'usd',
 *         value: '100',
 *       }],
 *     },
 *   },
 * });
 * ```
 *
 * @param client - The session client for the authenticated Account.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function configureAccountAction(client: SessionClient, request: ConfigureAccountActionRequest): ResultAsync<ConfigureAccountActionResult, UnexpectedError | UnauthenticatedError>;
/**
 * Enable account action.
 * The tipping action is not possible to modify.
 *
 * ```ts
 * const result = await enableAccountAction(sessionClient, {
 *   unknown: {
 *     params: [{
 *       key: 'usd',
 *       value: '100'
 *     }],
 *     address: evmAddress('0x1234…'),
 *   }
 * });
 * ```
 *
 * @param client - The session client for the authenticated Account.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function enableAccountAction(client: SessionClient, request: EnableAccountActionRequest): ResultAsync<EnableAccountActionResult, UnexpectedError | UnauthenticatedError>;
/**
 * Disable account action.
 * Not possible to disable the tipping action.
 *
 * ```ts
 * const result = await disableAccountAction(sessionClient, {
 *   unknown: {
 *     address: evmAddress('0x1234…'),
 *   }
 * });
 * ```
 *
 * @param client - The session client for the authenticated Account.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function disableAccountAction(client: SessionClient, request: DisableAccountActionRequest): ResultAsync<DisableAccountActionResult, UnexpectedError | UnauthenticatedError>;
/**
 * Execute account action.
 *
 * ```ts
 * const result = await executeAccountAction(sessionClient, {
 *   targetAccount: evmAddress('0x1234…'),
 *   params: {
 *     tipping: {
 *       value: '100',
 *       currency: evmAddress('0x5678…')
 *     }
 *   }
 * });
 * ```
 *
 * @param client - The session client for the authenticated Account.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function executeAccountAction(client: SessionClient, request: ExecuteAccountActionRequest): ResultAsync<ExecuteAccountActionResult, UnexpectedError | UnauthenticatedError>;

/**
 * Add Admins
 *
 * ```ts
 * const result = await addAdmins(sessionClient{
 *   admins: [evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5')],
 *   address: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 * });
 * ```
 *
 * @param client - The session client logged in as a builder.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function addAdmins(client: SessionClient, request: AddAdminsRequest): ResultAsync<AddAdminsResult, UnexpectedError | UnauthenticatedError>;
/**
 * Remove admins
 *
 * ```ts
 * const result = await removeAdmins(sessionClient{
 *   admins: [evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5')],
 *   address: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 * });
 * ```
 *
 * @param client - The session client for the authenticated Account.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function removeAdmins(client: SessionClient, request: RemoveAdminsRequest): ResultAsync<RemoveAdminsResult, UnexpectedError | UnauthenticatedError>;
/**
 * Fetch admins for.
 *
 * ```ts
 * const result = await fetchAdminsFor(anyClient, {
 *   address: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 * });
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The query request.
 * @returns The list of admins or empty if it does not exist.
 */
declare function fetchAdminsFor(client: AnyClient, request: AdminsForRequest): ResultAsync<Paginated<Admin>, UnexpectedError>;

/**
 * Fetch an App.
 *
 * Using a {@link SessionClient} will yield {@link App#operations}
 *
 * ```ts
 * const result = await fetchApp(anyClient, {
 *   address: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 * });
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The App query request.
 * @returns The App or `null` if it does not exist.
 */
declare function fetchApp(client: AnyClient, request: AppRequest): ResultAsync<App | null, UnexpectedError>;
/**
 * Fetch Apps.
 *
 * ```ts
 * const result = await fetchApps(anyClient, {
 *   filter: {
 *     managedBy: {
 *       address: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5')
 *     }
 *   },
 * });
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The query request.
 * @returns The list of Apps or empty if it does not exist.
 */
declare function fetchApps(client: AnyClient, request: AppsRequest): ResultAsync<Paginated<App>, UnexpectedError>;
/**
 * Fetch Groups linked to an App.
 *
 * ```ts
 * const result = await fetchAppGroups(anyClient, {
 *   app: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5')
 * });
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The query request.
 * @returns The list of groups for an App or empty if it does not exist.
 */
declare function fetchAppGroups(client: AnyClient, request: AppGroupsRequest): ResultAsync<Paginated<Group>, UnexpectedError>;
/**
 * Fetch Feeds linked to an App.
 *
 * ```ts
 * const result = await fetchAppFeeds(anyClient, {
 *   app: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5')
 * });
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The query request.
 * @returns The list of feeds for an App or empty if it does not exist.
 */
declare function fetchAppFeeds(client: AnyClient, request: AppFeedsRequest): ResultAsync<Paginated<AppFeed>, UnexpectedError>;
/**
 * Fetch Signers linked to an App.
 *
 * ```ts
 * const result = await fetchAppSigners(anyClient, {
 *   app: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5')
 * });
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The query request.
 * @returns The list of signers for an App or empty if it does not exist.
 */
declare function fetchAppSigners(client: AnyClient, request: AppSignersRequest): ResultAsync<Paginated<AppSigner>, UnexpectedError>;
/**
 * Fetch users using an App.
 *
 * ```ts
 * const result = await fetchAppUsers(anyClient, {
 *   app: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5')
 * });
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The query request.
 * @returns The list of users for an App or empty if it does not exist.
 */
declare function fetchAppUsers(client: AnyClient, request: AppUsersRequest): ResultAsync<Paginated<AppUser> | null, UnexpectedError>;
/**
 * Create an App
 *
 * ```ts
 * const result = await createApp(sessionClient, {
 *   defaultFeed: {
 *     globalFeed: true,
 *   },
 *   graph: {
 *     globalGraph: true,
 *   },
 *   namespace: {
 *     globalNamespace: true,
 *   },
 * });
 * ```
 *
 * @param client - The session client logged in as a builder.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function createApp(client: SessionClient, request: CreateAppRequest): ResultAsync<CreateAppResult, UnexpectedError | UnauthenticatedError>;
/**
 * Add feeds to an App
 *
 * ```ts
 * const result = await addAppFeeds(sessionClient, {
 *   feeds: [evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5')],
 *   app: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 * });
 * ```
 *
 * @param client - The session client logged in as a builder.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function addAppFeeds(client: SessionClient, request: AddAppFeedsRequest): ResultAsync<AddAppFeedsResult, UnexpectedError | UnauthenticatedError>;
/**
 * Add groups to an App
 *
 * ```ts
 * const result = await addAppGroups(sessionClient, {
 *   groups: [evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5')],
 *   app: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 * });
 * ```
 *
 * @param client - The session client logged in as a builder.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function addAppGroups(client: SessionClient, request: AddAppGroupsRequest): ResultAsync<AddAppGroupsResult, UnexpectedError | UnauthenticatedError>;
/**
 * Add signers to an App
 *
 * ```ts
 * const result = await addAppSigners(sessionClient, {
 *   signers: [evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5')],
 *   app: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 * });
 * ```
 *
 * @param client - The session client logged in as a builder.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function addAppSigners(client: SessionClient, request: AddAppSignersRequest): ResultAsync<AddAppSignersResult, UnexpectedError | UnauthenticatedError>;
/**
 * Remove feeds from an App
 *
 * ```ts
 * const result = await removeAppFeeds(sessionClient, {
 *   feeds: [evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5')],
 *   app: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 * });
 * ```
 *
 * @param client - The session client logged in as a builder.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function removeAppFeeds(client: SessionClient, request: RemoveAppFeedsRequest): ResultAsync<RemoveAppFeedsResult, UnexpectedError | UnauthenticatedError>;
/**
 * Remove groups from an App
 *
 * ```ts
 * const result = await removeAppGroups(sessionClient, {
 *   groups: [evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5')],
 *   app: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 * });
 * ```
 *
 * @param client - The session client logged in as a builder.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function removeAppGroups(client: SessionClient, request: RemoveAppGroupsRequest): ResultAsync<RemoveAppGroupsResult, UnexpectedError | UnauthenticatedError>;
/**
 * Remove signers from an App
 *
 * ```ts
 * const result = await removeAppSigners(sessionClient, {
 *   signers: [evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5')],
 *   app: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 * });
 * ```
 *
 * @param client - The session client logged in as a builder.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function removeAppSigners(client: SessionClient, request: RemoveAppSignersRequest): ResultAsync<RemoveAppSignersResult, UnexpectedError | UnauthenticatedError>;
/**
 * Set app Graph
 *
 * ```ts
 * const result = await setAppGraph(sessionClient, {
 *   graph: { globalGraph: true },
 *   app: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 * });
 * ```
 *
 * @param client - The session client logged in as a builder.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function setAppGraph(client: SessionClient, request: SetAppGraphRequest): ResultAsync<SetAppGraphResult, UnexpectedError | UnauthenticatedError>;
/**
 * Set default Feed for app
 *
 * ```ts
 * const result = await setDefaultAppFeed(sessionClient, {
 *   feed: { globalFeed: true },
 *   app: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 * });
 * ```
 *
 * @param client - The session client logged in as a builder.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function setDefaultAppFeed(client: SessionClient, request: SetDefaultAppFeedRequest): ResultAsync<SetDefaultAppFeedResult, UnexpectedError | UnauthenticatedError>;
/**
 * Set metadata for app
 *
 * ```ts
 * const result = await setAppMetadata(sessionClient, {
 *   metadataUri: uri("lens://4f91..."),
 *   app: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 * });
 * ```
 *
 * @param client - The session client logged in as a builder.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function setAppMetadata(client: SessionClient, request: SetAppMetadataRequest): ResultAsync<SetAppMetadataResult, UnexpectedError | UnauthenticatedError>;
/**
 * Set verification status for app
 *
 * ```ts
 * const result = await setAppVerification(sessionClient, {
 *   enabled: true,
 *   app: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 * });
 * ```
 *
 * @param client - The session client logged in as a builder.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function setAppVerification(client: SessionClient, request: SetAppVerificationRequest): ResultAsync<SetAppVerificationResult, UnexpectedError | UnauthenticatedError>;
/**
 * Set sponsorship for app
 *
 * ```ts
 * const result = await setAppSponsorship(sessionClient, {
 *   sponsorship: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 *   app: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 * });
 * ```
 *
 * @param client - The session client logged in as a builder.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function setAppSponsorship(client: SessionClient, request: SetAppSponsorshipRequest): ResultAsync<SetAppSponsorshipResult, UnexpectedError | UnauthenticatedError>;
/**
 * Set treasury for app
 *
 * ```ts
 * const result = await setAppTreasury(sessionClient, {
 *   treasury: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 *   app: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 * });
 * ```
 *
 * @param client - The session client logged in as a builder.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function setAppTreasury(client: SessionClient, request: SetAppTreasuryRequest): ResultAsync<SetAppTreasuryResult, UnexpectedError | UnauthenticatedError>;
/**
 * Set username namespace for app
 *
 * ```ts
 * const result = await setAppUsernameNamespace(sessionClient, {
 *   usernameNamespace: {
 *     custom: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 *   },
 *   app: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 * });
 * ```
 *
 * @param client - The session client logged in as a builder.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function setAppUsernameNamespace(client: SessionClient, request: SetAppUsernameNamespaceRequest): ResultAsync<SetAppUsernameNamespaceResult, UnexpectedError | UnauthenticatedError>;
/**
 * Add an authorization endpoint to an App
 *
 * ```ts
 * const result = await addAppAuthorizationEndpoint(sessionClient, {
 *   endpoint: uri('https://example.com/auth'),
 *   app: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 *   bearerToken: 'Qdy136748…',
 * });
 * ```
 *
 * @param client - The session client logged as a builder.
 * @param request - The mutation request.
 * @returns void.
 */
declare function addAppAuthorizationEndpoint(client: SessionClient, request: AddAppAuthorizationEndpointRequest): ResultAsync<void, UnexpectedError | UnauthenticatedError>;
/**
 * Remove an authorization endpoint to an App
 *
 * ```ts
 * const result = await removeAppAuthorizationEndpoint(sessionClient, {
 *   app: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 * });
 * ```
 *
 * @param client - The session client logged as a builder.
 * @param request - The mutation request.
 * @returns void.
 */
declare function removeAppAuthorizationEndpoint(client: SessionClient, request: RemoveAppAuthorizationEndpointRequest): ResultAsync<void, UnexpectedError | UnauthenticatedError>;
/**
 * Checks if the App has a server API key allocated.
 *
 * The actual value is redacted for security reasons.
 *
 * You MUST be logged-in as Builder and be the owner of the App.
 *
 * @param client  - The session client logged as a builder.
 * @param request - The query request.
 * @returns The server API key for the App.
 */
declare function fetchAppServerAPiKey(client: SessionClient, request: AppServerApiKeyRequest): ResultAsync<string | null, UnexpectedError | UnauthenticatedError>;
/**
 * Generate a new server API key for an App.
 *
 * You MUST be logged-in as Builder and be the owner of the App.
 *
 * @param client - The session client logged as a builder.
 * @param request - The mutation request.
 * @returns The new server API key for the App.
 */
declare function generateNewAppServerApiKey(client: SessionClient, request: GenerateNewAppServerApiKeyRequest): ResultAsync<ServerAPIKey, UnexpectedError | UnauthenticatedError>;

/**
 * Get the AuthenticatedSession associated with the authenticated Account.
 *
 * ```ts
 * const result = await currentSession(sessionClient);
 * ```
 *
 * @param client - The session client.
 * @returns The current AuthenticatedSession details.
 */
declare function currentSession(client: SessionClient): ResultAsync<AuthenticatedSession, UnauthenticatedError | UnexpectedError>;
/**
 * Revoke the authentication from the provided authentication ID.
 *
 * @remarks Use the {@link SessionClient#logout} method to logout instead.
 * It's unlikely you'll need to use this action directly.
 *
 * ```ts
 * const result = await revokeAuthentication(sessionClient, {
 *   authenticationId: 'f4dd2ea1-58d4-4210-86a2-67b7571f241a',
 * });
 * ```
 *
 * @param client - The session client for the authenticated Account.
 * @param request - The mutation request.
 * @returns Void if the operation was successful.
 */
declare function revokeAuthentication(client: SessionClient, request: RevokeAuthenticationRequest): ResultAsync<void, UnexpectedError | UnauthenticatedError>;
/**
 * Refresh the authentication tokens of the authenticated Account.
 *
 * ```ts
 * const result = await refresh(anyClient, {
 *   refreshToken: 'eyJhbGciOiJIUzI1NiIsInR5c…'
 * });
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The mutation request.
 * @returns The refreshed authentication tokens if the operation was successful.
 */
declare function refresh(client: AnyClient, request: RefreshRequest): ResultAsync<RefreshResult, UnexpectedError | UnauthenticatedError>;
/**
 * Fetch the authenticated sessions associated with the authenticated Account.
 *
 * ```ts
 * const result = await fetchAuthenticatedSessions(sessionClient);
 * ```
 *
 * @param client - The session client for the authenticated Account.
 * @param request - The query request.
 * @returns The paginated authenticated sessions associated with the authenticated Account.
 */
declare function fetchAuthenticatedSessions(client: SessionClient, request?: AuthenticatedSessionsRequest): ResultAsync<Paginated<AuthenticatedSession>, UnexpectedError>;
/**
 * Issue new authentication tokens from a valid Lens API v2 refresh token.
 *
 * Use this to seamlessly transition your users from Lens API v2 to Lens API v3 without
 * requiring them to re-authenticate.
 *
 * The HTTP Origin header MUST be present and match the app's domain.
 *
 * ```ts
 * const result = await legacyRolloverRefresh(sessionClient, {
 *   refreshToken: 'eyJhbGciOiJIUzI1NiIsInR5c…',
 *   app: evmAddress("0xe5439696f4057aF073c0FB2dc6e5e755392922e1"),
 * });
 * ```
 *
 * @param client - The client to use for the rollover refresh operation.
 * @param request - The mutation request.
 * @returns The refreshed authentication tokens if the operation was successful.
 */
declare function legacyRolloverRefresh(client: SessionClient, request: RolloverRefreshRequest): ResultAsync<RefreshResult, UnexpectedError | UnauthenticatedError>;
/**
 * Switch to another managed account.
 *
 * @remarks Use the {@link SessionClient#switchAccount} method to switch to an account instead.
 * It's unlikely you'll need to use this action directly.
 *
 * @param client - The current session client.
 * @param request - The query request.
 * @returns The authenticated tokens for the switched account.
 */
declare function switchAccount(client: SessionClient, request: SwitchAccountRequest): ResultAsync<SwitchAccountResult, UnauthenticatedError | UnexpectedError>;
/**
 * Retrieve the details of the authenticated Account.
 *
 * @param client - The session client for the authenticated Account.
 * @returns The details of the authenticated Account.
 */
declare function fetchMeDetails(client: SessionClient): ResultAsync<MeResult, UnauthenticatedError | UnexpectedError>;
/**
 * Get the last logged in account.
 *
 * ```ts
 * const result = await lastLoggedInAccount(anyClient, {
 *   address: evmAddress('0x90c8c68d0Abfb40D4fCD72316A65e42161520BC3'),
 * });
 * ```
 *
 * @param client - The session client.
 * @param request - The query request.
 * @returns The last logged in account.
 */
declare function lastLoggedInAccount(client: AnyClient, request: LastLoggedInAccountRequest): ResultAsync<Account | null, UnauthenticatedError | UnexpectedError>;

/**
 * Create a Feed
 *
 * ```ts
 * const result = await createFeed(sessionClient);
 * ```
 *
 * @param client - The session client logged in as a builder.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function createFeed(client: SessionClient, request: CreateFeedRequest): ResultAsync<CreateFeedResult, UnexpectedError | UnauthenticatedError>;
/**
 * Set Feed Metadata
 *
 * ```ts
 * const result = await setFeedMetadata(sessionClient, {
 *  feed: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 *  metadataUri: uri("lens://4f91..."),
 * });
 * ```
 *
 * @param client - The session client logged in as a builder.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function setFeedMetadata(client: SessionClient, request: SetFeedMetadataRequest): ResultAsync<SetFeedMetadataResult, UnexpectedError | UnauthenticatedError>;
/**
 * Fetch a Feed.
 *
 * ```ts
 * const result = await fetchFeed(anyClient, {
 *   feed: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 * });
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The Feed query request.
 * @returns The Feed or `null` if it does not exist.
 */
declare function fetchFeed(client: AnyClient, request: FeedRequest): ResultAsync<Feed | null, UnexpectedError>;
/**
 * Fetch Feeds.
 *
 * ```ts
 * const result = await fetchFeeds(anyClient, {
 *   filter: {
 *     managedBy: {
 *       address: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5')
 *     }
 *   },
 * });
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The Feeds query request.
 * @returns The list of Feeds or empty list if none exist.
 */
declare function fetchFeeds(client: AnyClient, request: FeedsRequest): ResultAsync<Paginated<Feed>, UnexpectedError>;
/**
 * Update feed rules.
 *
 * ```ts
 * const result = await updateFeedRules(sessionClient, {
 *   feed: evmAddress('0x1234…'),
 *   toAdd: {
 *     required: [{
 *       tokenGatedRule: {
 *         standard: TokenStandard.Erc20,
 *         currency: evmAddress('0x5678…'),
 *         value: '1.5', // Token value in its main unit
 *       }
 *     }],
 *     anyOf: [],
 *   }
 * });
 * ```
 *
 * @param client - The session client.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function updateFeedRules(client: SessionClient, request: UpdateFeedRulesRequest): ResultAsync<UpdateFeedRulesResult, UnauthenticatedError | UnexpectedError>;

/**
 * Follow an Account
 *
 * ```ts
 * const result = await follow(sessionClient, {
 *  account: evmAddress('0x90c8c68d0Abfb40D4fCD72316A65e42161520BC3')
 * });
 * ```
 *
 * @param client - The session client for the authenticated Account.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function follow(client: SessionClient, request: CreateFollowRequest): ResultAsync<FollowResult, UnexpectedError | UnauthenticatedError>;
/**
 * Unfollow an Account
 *
 * ```ts
 * const result = await unfollow(sessionClient, {
 *  account: evmAddress('0x90c8c68d0Abfb40D4fCD72316A65e42161520BC3')
 * });
 * ```
 *
 * @param client - The session client for the authenticated Account.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function unfollow(client: SessionClient, request: CreateUnfollowRequest): ResultAsync<UnfollowResult, UnexpectedError | UnauthenticatedError>;
/**
 * Fetch followers accounts.
 *
 * ```ts
 * const result = await fetchFollowers(anyClient, {
 *   account: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 * });
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The query request.
 * @returns List of followers accounts.
 */
declare function fetchFollowers(client: AnyClient, request: FollowersRequest): ResultAsync<Paginated<Follower>, UnexpectedError>;
/**
 * Fetch following accounts for an account.
 *
 * ```ts
 * const result = await fetchFollowing(anyClient, {
 *   account: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 * });
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The query request.
 * @returns List of accounts following.
 */
declare function fetchFollowing(client: AnyClient, request: FollowingRequest): ResultAsync<Paginated<Following>, UnexpectedError>;
/**
 * Fetch followers you know.
 *
 * ```ts
 * const result = await fetchFollowersYouKnow(anyClient, {
 *   observer: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 *   target: evmAddress('0xe5439696f4057aF073c0FB2dc6e5e755392922e1'),
 * });
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The query request.
 * @returns List of accounts following.
 */
declare function fetchFollowersYouKnow(client: AnyClient, request: FollowersYouKnowRequest): ResultAsync<Paginated<Follower>, UnexpectedError>;
/**
 * Fetch follow status.
 *
 * ```ts
 * const result = await fetchFollowStatus(anyClient,
 *   pairs: [
 *     {
 *       account: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 *       follower: evmAddress('0xe5439696f4057aF073c0FB2dc6e5e755392922e1'),
 *     }
 *   ],
 * });
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The query request.
 * @returns Status of the follow action.
 */
declare function fetchFollowStatus(client: AnyClient, request: FollowStatusRequest): ResultAsync<FollowStatusResult[], UnexpectedError>;

/**
 * Create Frame action typed data to be signed by user wallet
 *
 * @param request - The request object
 * @returns Typed data for Frame request
 * @experimental This function might change in the future release
 *
 * @example
 * ```ts
 * const result = await createFrameTypedData(anyClient, {
 *   transactionId: '0x0000000000000000000000000000000000000000',
 *   buttonIndex: 2,
 *   deadline: 1711038973,
 *   inputText: 'Hello, World!',
 *   account: '0x0000000000000000000000000000000000000000',
 *   post: '0x01-0x01',
 *   app: '0x0000000000000000000000000000000000000000,
 *   specVersion: '1.1.0',
 *   state: '{"counter":1,"idempotency_key":"431b8b38-eb4d-455b"}',
 *   url: 'https://mylensframe.xyz',
 * });
 * ```
 */
declare function createFrameTypedData(client: AnyClient, request: CreateFrameTypedDataRequest): ResultAsync<CreateFrameEip712TypedDataFragment, UnexpectedError>;
/**
 * Sign Frame action with Lens Manager if enabled
 *
 * ⚠️ Requires authenticated SessionClient.
 *
 * @param request - The request object
 * @returns Signature result
 * @experimental This function might change in the future release
 *
 * @example
 * ```ts
 * const result = await signFrameAction(sessionClient, {
 *   transactionId: '0x0000000000000000000000000000000000000000',
 *   buttonIndex: 2,
 *   inputText: 'Hello, World!',
 *   account: '0x0000000000000000000000000000000000000000',
 *   post: '0x01-0x01',
 *   app: '0x0000000000000000000000000000000000000000,
 *   specVersion: '1.1.0',
 *   state: '{"counter":1,"idempotency_key":"431b8b38-eb4d-455b"}',
 *   url: 'https://mylensframe.xyz',
 * });
 * ```
 */
declare function signFrameAction(client: SessionClient, request: SignFrameActionRequest): ResultAsync<FrameLensManagerSignatureResultFragment, UnexpectedError | UnauthenticatedError>;
/**
 * Verify Frame signature
 *
 * @param request - The request object
 * @returns Verification result
 * @experimental This function might change in the future release
 *
 * @example
 * ```ts
 * const result = await verifyFrameSignature(anyClient, {
 *   identityToken: identityToken,
 *   signature: data.signature,
 *   signedTypedData: data.signedTypedData,
 * });
 * ```
 */
declare function verifyFrameSignature(client: AnyClient, request: VerifyFrameSignatureRequest): ResultAsync<FrameVerifySignatureResult, UnexpectedError>;

/**
 * @deprecated Use `fetchBalancesBulk` instead.
 *
 * Fetch balances for the authenticated Account.
 *
 * ```ts
 * const result = await fetchBalancesBulk(sessionClient, {
 *   includeNative: true,
 *   tokens: [
 *     evmAddress("0x12345…"),
 *     evmAddress("0x67890…"),
 *   ]
 * });
 * ```
 *
 * @param client - The session client for the authenticated Account.
 * @param request - The query request.
 * @returns The list of balance results.
 */
declare function fetchAccountBalances(client: SessionClient, request: AccountBalancesRequest): ResultAsync<AnyAccountBalance[], UnauthenticatedError | UnexpectedError>;
/**
 * Fetch balances for the provided addresses. Needs to be authenticated to execute.
 *
 * ```ts
 * const result = await fetchBalancesBulk(anyClient, {
 *   address: evmAddress("0x12345…"),
 *   includeNative: true,
 *   tokens: [
 *     evmAddress("0x45678…"),
 *     evmAddress("0x90123…"),
 *   ],
 * });
 * ```
 *
 * @param client - The session client for the authenticated Account.
 * @param request - The query request.
 * @returns The list of balance results for the provided addresses.
 */
declare function fetchBalancesBulk(client: SessionClient, request: BalancesBulkRequest): ResultAsync<AnyBalance[], UnauthenticatedError | UnexpectedError>;
/**
 * Withdraw funds from the authenticated Account.
 *
 * ```ts
 * const result = await withdraw(sessionClient, {
 *   native: bigDecimal(42.5),
 * });
 * ```
 *
 * @param client - The session client for the authenticated Account.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function withdraw(sessionClient: SessionClient, request: WithdrawRequest): ResultAsync<WithdrawResult, UnexpectedError | UnauthenticatedError>;
/**
 * Deposit funds into the authenticated Account.
 *
 * ```ts
 * const result = await deposit(sessionClient, {
 *   native: bigDecimal(42.5),
 * });
 * ```
 *
 * @param client - The session client for the authenticated Account.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function deposit(sessionClient: SessionClient, request: DepositRequest): ResultAsync<DepositResult, UnexpectedError | UnauthenticatedError>;
/**
 * Convert native tokens held in the authenticated Lens Account to their ERC20 equivalent. For example:
 * - Mainnet: GHO -> WGHO
 * - Testnet: GRASS -> WGRASS
 *
 * ```ts
 * const result = await wrapTokens(sessionClient, {
 *   amount: bigDecimal(42.5),
 * });
 * ```
 *
 * @param client - The session client for the authenticated Account.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function wrapTokens(sessionClient: SessionClient, request: WrapTokensRequest): ResultAsync<WrapTokensResult, UnexpectedError | UnauthenticatedError>;
/**
 * Unwrap wrapped native tokens held in the authenticated Lens Account. For example:
 * - Mainnet: WGHO -> GHO
 * - Testnet: WGRASS -> GRASS
 *
 * ```ts
 * const result = await unwrapTokens(sessionClient, {
 *   amount: bigDecimal(42.5),
 * });
 * ```
 *
 * @param client - The session client for the authenticated Account.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function unwrapTokens(sessionClient: SessionClient, request: UnwrapTokensRequest): ResultAsync<UnwrapTokensResult, UnexpectedError | UnauthenticatedError>;

/**
 * Create a Graph
 *
 * ```ts
 * const result = await createGraph(sessionClient);
 * ```
 *
 * @param client - The session client logged in as a builder.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function createGraph(client: SessionClient, request: CreateGraphRequest): ResultAsync<CreateGraphResult, UnexpectedError | UnauthenticatedError>;
/**
 * Set Graph Metadata
 *
 * ```ts
 * const result = await setGraphMetadata(sessionClient, {
 *  graph: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 *  metadataUri: uri("lens://4f91..."),
 * });
 * ```
 *
 * @param client - The session client logged in as a builder.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function setGraphMetadata(client: SessionClient, request: SetGraphMetadataRequest): ResultAsync<SetGraphMetadataResult, UnexpectedError | UnauthenticatedError>;
/**
 * Fetch a Graph.
 *
 * ```ts
 * const result = await fetchGraph(anyClient, {
 *   graph: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 * });
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The Graph query request.
 * @returns The Graph or `null` if it does not exist.
 */
declare function fetchGraph(client: AnyClient, request: GraphRequest): ResultAsync<Graph | null, UnexpectedError>;
/**
 * Fetch Graphs.
 *
 * ```ts
 * const result = await fetchGraphs(anyClient, {
 *   filter: {
 *     managedBy: {
 *       address: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5')
 *     }
 *   },
 * });
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The Graphs query request.
 * @returns The list of Graphs or empty list if none exist.
 */
declare function fetchGraphs(client: AnyClient, request: GraphsRequest): ResultAsync<Paginated<Graph>, UnexpectedError>;
/**
 * Update graph rules.
 *
 * ```ts
 * const result = await updateGraphRules(sessionClient, {
 *   graph: evmAddress('0x1234…'),
 *   toAdd: {
 *     required: [{
 *       tokenGatedRule: {
 *         standard: TokenStandard.Erc20,
 *         currency: evmAddress('0x5678…'),
 *         value: '1.5', // Token value in its main unit
 *       }
 *     }],
 *     anyOf: [],
 *   }
 * });
 * ```
 *
 * @param client - The session client.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function updateGraphRules(client: SessionClient, request: UpdateGraphRulesRequest): ResultAsync<UpdateGraphRulesResult, UnauthenticatedError | UnexpectedError>;

/**
 * Create a Group
 *
 * ```ts
 * const result = await createGroup(sessionClient, {
 *   metadataUri: uri("lens://4f91..."),
 * });
 * ```
 *
 * @param client - The session client for the authenticated Account.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function createGroup(client: SessionClient, request: CreateGroupRequest): ResultAsync<CreateGroupResult, UnexpectedError | UnauthenticatedError>;
/**
 * Set Group Metadata
 *
 * ```ts
 * const result = await setGroupMetadata(sessionClient, {
 *  group: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 *  metadataUri: uri("lens://4f91..."),
 * });
 * ```
 *
 * @param client - The session client for the authenticated Account.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function setGroupMetadata(client: SessionClient, request: SetGroupMetadataRequest): ResultAsync<SetGroupMetadataResult, UnexpectedError | UnauthenticatedError>;
/**
 * Join a Group
 *
 * ```ts
 * const result = await joinGroup(sessionClient, {
 *   group: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 * });
 * ```
 *
 * @param client - The session client for the authenticated Account.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function joinGroup(client: SessionClient, request: JoinGroupRequest): ResultAsync<JoinGroupResult, UnexpectedError | UnauthenticatedError>;
/**
 * Leave a Group
 *
 * ```ts
 * const result = await leaveGroup(sessionClient, {
 *   group: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 * });
 * ```
 *
 * @param client - The session client for the authenticated Account.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function leaveGroup(client: SessionClient, request: LeaveGroupRequest): ResultAsync<LeaveGroupResult, UnexpectedError | UnauthenticatedError>;
/**
 * Fetch a Group.
 *
 * ```ts
 * const result = await fetchGroup(anyClient, {
 *   group: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 * });
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The query request.
 * @returns The Group or `null` if it does not exist.
 */
declare function fetchGroup(client: AnyClient, request: GroupRequest): ResultAsync<Group | null, UnexpectedError>;
/**
 * Fetch groups.
 *
 * ```ts
 * const result = await fetchGroups(anyClient);
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The query request.
 * @returns The list of groups.
 */
declare function fetchGroups(client: AnyClient, request?: GroupsRequest): ResultAsync<Paginated<Group>, UnexpectedError>;
/**
 * Fetch group members.
 *
 * ```ts
 * const result = await fetchGroupMembers(anyClient, {
 *   group: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 * });
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The query request.
 * @returns The list of members for the group.
 */
declare function fetchGroupMembers(client: AnyClient, request: GroupMembersRequest): ResultAsync<Paginated<GroupMember>, UnexpectedError>;
/**
 * Fetch stats for a group.
 *
 * ```ts
 * const result = await fetchGroupStats(anyClient, {
 *   group: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 * });
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The query request.
 * @returns The stats for the group.
 */
declare function fetchGroupStats(client: AnyClient, request: GroupStatsRequest): ResultAsync<GroupStatsResponse | null, UnexpectedError>;
/**
 * Fetch banned accounts for a group.
 *
 * ```ts
 * const result = await fetchGroupBannedAccounts(anyClient, {
 *   group: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 * });
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The query request.
 * @returns The list of banned accounts for the group.
 */
declare function fetchGroupBannedAccounts(client: AnyClient, request: GroupBannedAccountsRequest): ResultAsync<Paginated<GroupBannedAccount>, UnexpectedError>;
/**
 * Fetch membership requests for a group (only admin/owner).
 *
 * ```ts
 * const result = await fetchGroupMembershipRequests(sessionClient, {
 *   group: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 * });
 * ```
 *
 * @param client - The session client for the authenticated Account.
 * @param request - The query request.
 * @returns The list of membership requests for the group.
 */
declare function fetchGroupMembershipRequests(client: SessionClient, request: GroupMembershipRequestsRequest): ResultAsync<Paginated<GroupMembershipRequest>, UnexpectedError>;
/**
 * Update group rules.
 *
 * ```ts
 * const result = await updateGroupRules(sessionClient, {
 *   group: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 *   toAdd: {
 *     required: [{
 *       membershipApprovalRule: {
 *         enable: true
 *       }
 *     }]
 *     anyOf: [],
 *   }
 * });
 * ```
 *
 * @param client - The session client for the authenticated Account.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function updateGroupRules(client: SessionClient, request: UpdateGroupRulesRequest): ResultAsync<UpdateGroupRulesResult, UnexpectedError | UnauthenticatedError>;
/**
 * Approve group membership requests.
 *
 * ```ts
 * const result = await approveGroupMembershipRequests(sessionClient, {
 *   group: evmAddress('0xe2f2…'),
 *   accounts: [evmAddress('0x4f91…'), evmAddress('0x4f92…')],
 * });
 * ```
 *
 * @param client - The session client for the authenticated Account.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function approveGroupMembershipRequests(client: SessionClient, request: ApproveGroupMembershipRequest): ResultAsync<ApproveGroupMembershipResult, UnexpectedError | UnauthenticatedError>;
/**
 * Remove account from a group.
 *
 * ```ts
 * const result = await removeGroupMembers(sessionClient, {
 *   group: evmAddress('0xe2f…'),
 *   accounts: [evmAddress('0x4f91…'), evmAddress('0x4f92…')],
 *   ban: true, // contextually bans the accounts from rejoining
 * });
 * ```
 *
 * @param client - The session client for the authenticated Account.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function removeGroupMembers(client: SessionClient, request: RemoveGroupMembersRequest): ResultAsync<RemoveGroupMembersResult, UnexpectedError | UnauthenticatedError>;
/**
 * Request membership for a group.
 *
 * ```ts
 * const result = await requestGroupMembership(sessionClient, {
 *   group: evmAddress('0xe2f…'),
 * });
 * ```
 *
 * @param client - The session client for the authenticated Account.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function requestGroupMembership(client: SessionClient, request: RequestGroupMembershipRequest): ResultAsync<RequestGroupMembershipResult, UnexpectedError | UnauthenticatedError>;
/**
 * Cancel and existing request to be part of a group.
 *
 * ```ts
 * const result = await cancelGroupMembershipRequest(sessionClient, {
 *   group: evmAddress('0xe2f…'),
 * });
 * ```
 *
 * @param client - The session client for the authenticated Account.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function cancelGroupMembershipRequest(client: SessionClient, request: CancelGroupMembershipRequestRequest): ResultAsync<CancelGroupMembershipRequestResult, UnexpectedError | UnauthenticatedError>;
/**
 * Reject group membership requests.
 *
 * You must be the owner or admin of the group to reject membership requests.
 *
 * ```ts
 * const result = await rejectGroupMembershipRequests(sessionClient, {
 *   group: evmAddress('0xe2f…'),
 *   accounts: [evmAddress('0x4f91…'), evmAddress('0x4f92…')],
 * });
 * ```
 *
 * @param client - The session client for the authenticated Account.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function rejectGroupMembershipRequests(client: SessionClient, request: RejectGroupMembershipRequest): ResultAsync<RejectGroupMembershipResult, UnexpectedError | UnauthenticatedError>;
/**
 * Ban accounts from a group. These accounts will not be able to join the group.
 *
 * You must be the owner or admin of the group to ban accounts.
 *
 * ```ts
 * const result = await banGroupAccount(sessionClient, {
 *   group: evmAddress('0xe2f…'),
 *   accounts: [evmAddress('0x4f91…'), evmAddress('0x4f92…')],
 * });
 * ```
 *
 * @param client - The session client for the authenticated Account.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function banGroupAccounts(client: SessionClient, request: BanGroupAccountsRequest): ResultAsync<BanGroupAccountsResult, UnexpectedError | UnauthenticatedError>;
/**
 * Unban accounts from a group.
 *
 * You must be the owner or admin of the group to unban accounts.
 *
 * ```ts
 * const result = await unbanGroupAccounts(sessionClient, {
 *   group: evmAddress('0xe2f…'),
 *   accounts: [evmAddress('0x4f91…'), evmAddress('0x4f92…')],
 * });
 * ```
 *
 * @param client - The session client for the authenticated Account.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function unbanGroupAccounts(client: SessionClient, request: UnbanGroupAccountsRequest): ResultAsync<UnbanGroupAccountsResult, UnexpectedError | UnauthenticatedError>;

/**
 * Given a list of account balances, find the native amount.
 *
 * @experimental This function is subject to change.
 * @param balances - A list of account balances as returned by the {@link fetchAccountBalances} action.
 * @returns The native amount.
 */
declare function findNativeAmount(balances: AnyAccountBalance[]): Result<NativeAmount, InvariantError | UnexpectedError>;
/**
 * Given a list of account balances, find the ERC20 entry for a given token.
 *
 * @experimental This function is subject to change.
 * @param token - The token address.
 * @param balances - A list of account balances as returned by the {@link fetchAccountBalances} action.
 * @returns The ERC20 amount.
 */
declare function findErc20Amount(token: EvmAddress, balances: AnyAccountBalance[]): Result<Erc20Amount, InvariantError | UnexpectedError>;

/**
 * Fetch the indexing status of metadata.
 *
 * ```ts
 * const result = await refreshMetadataStatus(anyClient, {
 *   id: uuid("a0a88a62-377f-46eb-a1ec-ca6597aef164")
 * });
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The query request.
 * @returns The indexing status of the metadata.
 */
declare function refreshMetadataStatus(client: AnyClient, request: RefreshMetadataStatusRequest): ResultAsync<RefreshMetadataStatusResult, UnexpectedError>;
/**
 * Refresh the metadata for a given entity.
 *
 * ```ts
 * const result = await refreshMetadata(anyClient, {
 *   entity: {
 *     post: postId('42'),
 *   },
 * });
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The mutation request.
 * @returns - UUID to track the metadata refresh.
 */
declare function refreshMetadata(client: AnyClient, request: RefreshMetadataRequest): ResultAsync<RefreshMetadataResult, UnauthenticatedError | UnexpectedError>;
/**
 * Given a metadata id, wait for the metadata to be either confirmed or rejected by the Lens API.
 *
 * @param client - Any Lens client.
 * @param id - The metadata id to wait for.
 * @returns The metadata id if the metadata was confirmed or an error if the transaction was rejected.
 */
declare function waitForMetadata(client: AnyClient, id: UUID): ResultAsync<UUID, MetadataIndexingError | UnexpectedError>;

/**
 * Health check query.
 *
 * ```ts
 * const result = await health(anyClient);
 * ```
 *
 * @param client - Any Lens client.
 * @returns True or false
 */
declare function health(client: AnyClient): ResultAsync<boolean, UnexpectedError>;
/**
 * Fetch an Access Control details.
 *
 * ```ts
 * const result = await fetchAccessControl(anyClient, {
 *   address: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 * });
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The Access Control query request.
 * @returns The Access Control or `null` if it does not exist.
 */
declare function fetchAccessControl(client: AnyClient, request: AccessControlRequest): ResultAsync<AccessControlResult | null, UnexpectedError>;
/**
 * Fetch list of token distributions received by the authenticated account.
 *
 * ```ts
 * const result = await fetchTokenDistributions(sessionClient);
 * ```
 *
 * @param client - Lens SessionClient.
 * @param request - The query request.
 * @returns List of token distributions for the authenticated account was rewarded with.
 */
declare function fetchTokenDistributions(client: SessionClient, request?: TokenDistributionsRequest): ResultAsync<Paginated<TokenDistribution>, UnexpectedError>;

/**
 * Fetch account recommendations from ML.
 *
 * ```ts
 * const result = await fetchAccountRecommendations(anyClient, {
 *   account: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 * });
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The query request.
 * @returns The list accounts recommended.
 */
declare function fetchAccountRecommendations(client: AnyClient, request: AccountRecommendationsRequest): ResultAsync<Paginated<Account>, UnexpectedError>;
/**
 * Fetch posts for you from ML.
 *
 * ```ts
 * const result = await fetchPostsForYou(anyClient, {
 *   account: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 * });
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The query request.
 * @returns The list of recommended posts.
 */
declare function fetchPostsForYou(client: AnyClient, request: PostsForYouRequest): ResultAsync<Paginated<PostForYou>, UnexpectedError>;
/**
 * Fetch posts to explore.
 *
 * ```ts
 * const result = await fetchPostsToExplore(anyClient);
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The query request.
 * @returns The list of posts to explore.
 */
declare function fetchPostsToExplore(client: AnyClient, request: PostsExploreRequest): ResultAsync<Paginated<Post>, UnexpectedError>;
/**
 * Dismiss recommended accounts.
 *
 * ```ts
 * const result = await dismissRecommendedAccounts(sessionClient, {
 *   accounts: [evmAddress('0xe2f2...')],
 * });
 * ```
 *
 * @param client - Session Lens client.
 * @param request - The list of accounts to dismiss.
 * @returns - void
 */
declare function dismissRecommendedAccounts(client: SessionClient, request: DismissRecommendedAccountsRequest): ResultAsync<void, UnexpectedError | UnauthenticatedError>;
/**
 * Flag a post as not of interest.
 *
 * ```ts
 * const result = await addPostNotInterested(sessionClient, {
 *   post: postID('34fdasd...'),
 * });
 * ```
 *
 * @param client - Session Lens client.
 * @param request - The post to add as not interested.
 * @returns - void
 */
declare function addPostNotInterested(client: SessionClient, request: PostNotInterestedRequest): ResultAsync<void, UnexpectedError | UnauthenticatedError>;
/**
 * Undo a previous decision to flag a post as uninteresting.
 *
 * ```ts
 * const result = await undoPostNotInterested(sessionClient, {
 *   post: postID('34fdasd...'),
 * });
 * ```
 *
 * @param client - Session Lens client.
 * @param request - The post to remove as not interested.
 * @returns - void
 */
declare function undoPostNotInterested(client: SessionClient, request: PostNotInterestedRequest): ResultAsync<void, UnexpectedError | UnauthenticatedError>;

/**
 * Create a Namespace
 *
 * ```ts
 * const result = await createUsernameNamespace(sessionClient, {
 *   symbol: 'NAME',
 *   namespace: 'custom-namespace',
 * });
 * ```
 *
 * @param client - The session client logged in as a builder.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function createUsernameNamespace(client: SessionClient, request: CreateUsernameNamespaceRequest): ResultAsync<CreateUsernameNamespaceResult, UnexpectedError | UnauthenticatedError>;
/**
 * Set Namespace Metadata
 *
 * ```ts
 * const result = await setNamespaceMetadata(sessionClient, {
 *   namespace: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 *   metadataUri: uri("lens://4f91..."),
 * });
 * ```
 *
 * @param client - The session client logged in as a builder.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function setNamespaceMetadata(client: SessionClient, request: SetNamespaceMetadataRequest): ResultAsync<SetNamespaceMetadataResult, UnexpectedError | UnauthenticatedError>;
/**
 * Fetch a Namespace.
 *
 * ```ts
 * const result = await fetchNamespace(anyClient, {
 *   namespace: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 * });
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The query request.
 * @returns The UsernameNamespace or `null` if it does not exist.
 */
declare function fetchNamespace(client: AnyClient, request: NamespaceRequest): ResultAsync<UsernameNamespace | null, UnexpectedError>;
/**
 * Fetch Namespaces.
 *
 * ```ts
 * const result = await fetchNamespaces(anyClient, {
 *   filter: {
 *     managedBy: {
 *       address: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5')
 *     }
 *   },
 * });
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The query request.
 * @returns The list of Namespaces or empty list if none exist.
 */
declare function fetchNamespaces(client: AnyClient, request: NamespacesRequest): ResultAsync<Paginated<UsernameNamespace>, UnexpectedError>;
/**
 * Update namespace rules.
 *
 * ```ts
 * const result = await updateNamespaceRules(sessionClient, {
 *   namespace: evmAddress('0x1234…'),
 *   toAdd: {
 *     required: [{
 *       tokenGatedRule: {
 *         standard: TokenStandard.Erc20,
 *         currency: evmAddress('0x5678…'),
 *         value: '1.5', // Token value in its main unit
 *       }
 *     }],
 *     anyOf: [],
 *   }
 * });
 * ```
 *
 * @param client - The session client.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function updateNamespaceRules(client: SessionClient, request: UpdateNamespaceRulesRequest): ResultAsync<UpdateNamespaceRulesResult, UnauthenticatedError | UnexpectedError>;
/**
 * Update reserved usernames in a namespace.
 *
 * ```ts
 * const result = await updateReservedUsernames(sessionClient, {
 *   namespace: evmAddress('0x1234…'),
 *   toRelease: ['alice', 'bob'],
 *   toReserve: ['charlie', 'dave'],
 * });
 * ```
 *
 * @param client - The session client.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function updateReservedUsernames(client: SessionClient, request: UpdateReservedUsernamesRequest): ResultAsync<UpdateReservedUsernamesResult, UnauthenticatedError | UnexpectedError>;
/**
 * Fetch all reserved usernames in a namespace.
 *
 * ```ts
 * const result = await fetchNamespaceReservedUsernames(anyClient, {
 *   namespace: evmAddress('0x1234…'),
 * });
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The query request.
 * @returns The list of reserved usernames or empty list if none exist.
 */
declare function fetchNamespaceReservedUsernames(client: AnyClient, request: NamespaceReservedUsernamesRequest): ResultAsync<Paginated<UsernameReserved>, UnexpectedError>;

/**
 * Fetch notifications for the authenticated Account.
 *
 * ```ts
 * const result = await fetchNotifications(sessionClient);
 * ```
 *
 * @param client - The session client for the authenticated Account.
 * @param request - The query request.
 * @returns Paginated notifications.
 */
declare function fetchNotifications(client: SessionClient, request?: NotificationsRequest): ResultAsync<Paginated<Notification>, UnexpectedError>;

/**
 * Create a new Post.
 *
 * ```ts
 * const result = await post(sessionClient, {
 *   contentUri: uri('https://example.com'),
 * });
 * ```
 *
 * @param client - The session client.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function post(client: SessionClient, request: CreatePostRequest): ResultAsync<PostResult, UnauthenticatedError | UnexpectedError>;
/**
 * Repost a Post.
 *
 * ```ts
 * const result = await repost(sessionClient, {
 *   post: postId('42'),
 * });
 * ```
 *
 * @param client - The session client.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function repost(client: SessionClient, request: CreateRepostRequest): ResultAsync<PostResult, UnauthenticatedError | UnexpectedError>;
/**
 * Edit a Post.
 *
 * ```ts
 * const result = await editPost(sessionClient, {
 *   post: postId('42'),
 *   contentUri: uri('https://example.com'),
 * });
 * ```
 *
 * @param client - The session client.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function editPost(client: SessionClient, request: EditPostRequest): ResultAsync<PostResult, UnauthenticatedError | UnexpectedError>;
/**
 * Delete a Post.
 *
 * ```ts
 * const result = await deletePost(sessionClient, {
 *   post: postId('42'),
 * });
 * ```
 *
 * @param client - The session client.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function deletePost(client: SessionClient, request: DeletePostRequest): ResultAsync<DeletePostResult, UnauthenticatedError | UnexpectedError>;
/**
 * React to a post.
 *
 * ```ts
 * const result = await addReaction(sessionClient, {
 *   post: postId('42'),
 *   reaction: "UPVOTE" | "DOWNVOTE",
 * });
 * ```
 *
 * @param client - The session client.
 * @param request - The mutation request.
 * @returns Success boolean if reaction was added or error with a reason.
 */
declare function addReaction(client: SessionClient, request: AddReactionRequest): ResultAsync<AddReactionResult, UnauthenticatedError | UnexpectedError>;
/**
 * Undo reaction from a post.
 *
 * ```ts
 * const result = await undoReaction(sessionClient, {
 *   post: postId('42'),
 *   reaction: "UPVOTE" | "DOWNVOTE",
 * });
 * ```
 *
 * @param client - The session client.
 * @param request - The mutation request.
 * @returns Success boolean if reaction was removed or error with a reason.
 */
declare function undoReaction(client: SessionClient, request: UndoReactionRequest): ResultAsync<UndoReactionResult, UnauthenticatedError | UnexpectedError>;
/**
 * Bookmark a post.
 *
 * ```ts
 * const result = await bookmarkPost(sessionClient, {
 *   post: postId('42'),
 * });
 * ```
 *
 * @param client - The session client.
 * @param request - The mutation request.
 * @returns void
 */
declare function bookmarkPost(client: SessionClient, request: BookmarkPostRequest): ResultAsync<void, UnauthenticatedError | UnexpectedError>;
/**
 * Undo bookmark from a post.
 *
 * ```ts
 * const result = await undoBookmarkPost(sessionClient, {
 *   post: postId('42'),
 * });
 * ```
 *
 * @param client - The session client.
 * @param request - The mutation request.
 * @returns void
 */
declare function undoBookmarkPost(client: SessionClient, request: UndoBookmarkPostRequest): ResultAsync<void, UnauthenticatedError | UnexpectedError>;
/**
 * Hide a reply.
 *
 * ```ts
 * const result = await hideReply(sessionClient, {
 *   post: postId('42'),
 * });
 * ```
 *
 * @param client - The session client.
 * @param request - The mutation request.
 * @returns void
 */
declare function hideReply(client: SessionClient, request: HideReplyRequest): ResultAsync<void, UnauthenticatedError | UnexpectedError>;
/**
 * Unhide a reply.
 *
 * ```ts
 * const result = await unhideReply(sessionClient, {
 *   post: postId('42'),
 * });
 * ```
 *
 * @param client - The session client.
 * @param request - The mutation request.
 * @returns void
 */
declare function unhideReply(client: SessionClient, request: UnhideReplyRequest): ResultAsync<void, UnauthenticatedError | UnexpectedError>;
/**
 * Report a post
 *
 * ```ts
 * const result = await reportPost(sessionClient, {
 *   reason: "SCAM",
 *   post: postId('1234…'),
 * });
 * ```
 *
 * @param client - The session client.
 * @param request - The mutation request.
 * @returns void
 */
declare function reportPost(client: SessionClient, request: ReportPostRequest): ResultAsync<void, UnauthenticatedError | UnexpectedError>;
/**
 * Update post rules.
 *
 * ```ts
 * const result = await updatePostRules(sessionClient, {
 *   post: postId('42…'),
 *   toAdd: {
 *     anyOf: [{
 *       followersOnlyRule: {
 *         graph: evmAddress('0x1234…'),
 *       }
 *     }]
 *     required: [],
 *   }
 * });
 * ```
 *
 * @param client - The session client.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function updatePostRules(client: SessionClient, request: UpdatePostRulesRequest): ResultAsync<UpdatePostRulesResult, UnauthenticatedError | UnexpectedError>;

/**
 * Fetch a Post.
 *
 * Using a {@link SessionClient} will yield {@link Post#operations}
 * and {@link Account#operations} specific to the authenticated Account.
 *
 * ```ts
 * const result = await fetchPost(anyClient, {
 *   post: postId('42')
 * });
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The query request.
 * @returns The Post or `null` if it does not exist.
 */
declare function fetchPost(client: AnyClient, request: PostRequest): ResultAsync<AnyPost | null, UnexpectedError>;
/**
 * Fetch paginated Posts.
 *
 * Using a {@link SessionClient} will yield {@link Post#operations}
 * and {@link Account#operations} specific to the authenticated Account.
 *
 * ```ts
 * const result = await fetchPosts(anyClient, {
 *   filter: {
 *     authors: [evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5')],
 *   }
 * });
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The query request.
 * @returns The paginated list of Posts.
 */
declare function fetchPosts(client: AnyClient, request: PostsRequest): ResultAsync<Paginated<AnyPost>, UnexpectedError>;
/**
 * Fetch availale Post Action contracts.
 *
 * ```ts
 * const result = await fetchPostActionContracts(anyClient);
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The query request.
 * @returns The list of actions available.
 */
declare function fetchPostActionContracts(client: AnyClient, request?: PostActionContractsRequest): ResultAsync<Paginated<PostActionContract>, UnexpectedError>;
/**
 * Fetch reactions for a post.
 *
 * ```ts
 * const result = await fetchPostReactions(anyClient, {
 *   post: postId('42'),
 * });
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The query request.
 * @returns The list of reactions for the post.
 */
declare function fetchPostReactions(client: AnyClient, request: PostReactionsRequest): ResultAsync<Paginated<AccountPostReaction>, UnexpectedError>;
/**
 * Fetch bookmarked posts.
 *
 * ```ts
 * const result = await fetchPostBookmarks(anyClient);
 * ```
 *
 * @param client - Session Lens client.
 * @param request - The query request.
 * @returns The list of bookmarked posts.
 */
declare function fetchPostBookmarks(client: SessionClient, request?: PostBookmarksRequest): ResultAsync<Paginated<AnyPost>, UnexpectedError | UnauthenticatedError>;
/**
 * Fetch references to a post.
 *
 * ```ts
 * const result = await fetchPostReferences(anyClient, {
 *   referencedTypes: [PostReferenceType.CommentOn],
 *   referencedPost: postId('42'),
 * });
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The query request.
 * @returns The list of posts references.
 */
declare function fetchPostReferences(client: AnyClient, request: PostReferencesRequest): ResultAsync<Paginated<AnyPost>, UnexpectedError | UnauthenticatedError>;
/**
 * Fetch post tags.
 *
 * ```ts
 * const result = await fetchPostTags(anyClient, {
 *   filter: {
 *     feeds: { globalFeed: true },
 *   },
 * });
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The query request.
 * @returns The list of post tags.
 */
declare function fetchPostTags(client: AnyClient, request: PostTagsRequest): ResultAsync<Paginated<PostTag>, UnexpectedError>;
/**
 * Fetch post reaction status.
 *
 * ```ts
 * const result = await fetchPostReactionStatus(anyClient, {
 *   pairs: [{
 *     account: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5')],
 *     post: postId('42'),
 *   }],
 * });
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The query request.
 * @returns The list of post reaction status.
 */
declare function fetchPostReactionStatus(client: AnyClient, request: PostReactionStatusRequest): ResultAsync<PostReactionStatus[], UnexpectedError>;
/**
 * Fetch who referenced post.
 *
 * ```ts
 * const result = await fetchWhoReferencedPost(anyClient, {
 *   referenceTypes: [PostReferenceType.CommentOn]
 *   post: postId('42'),
 * });
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The query request.
 * @returns The list of accounts who referenced the post.
 */
declare function fetchWhoReferencedPost(client: AnyClient, request: WhoReferencedPostRequest): ResultAsync<Paginated<Account>, UnexpectedError>;
/**
 * Fetch who executed an action on a Post.
 *
 * ```ts
 * const result = await fetchWhoExecutedActionOnPost(anyClient, {
 *   post: postId('42'),
 * });
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The query request.
 * @returns The list of accounts who acted on the post.
 */
declare function fetchWhoExecutedActionOnPost(client: AnyClient, request: WhoExecutedActionOnPostRequest): ResultAsync<Paginated<PostExecutedActions>, UnexpectedError>;
/**
 * Fetch who executed an action on an Account.
 *
 * ```ts
 * const result = await fetchWhoExecutedActionOnAccount(anyClient, {
 *   account: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 * });
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The query request.
 * @returns The list of accounts who acted on the post.
 */
declare function fetchWhoExecutedActionOnAccount(client: AnyClient, request: WhoExecutedActionOnAccountRequest): ResultAsync<Paginated<AccountExecutedActions>, UnexpectedError>;
/**
 * Fetch post edits.
 *
 * ```ts
 * const result = await fetchPostEdits(anyClient, {
 *   post:  postId('42'),
 * });
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The query request.
 * @returns The list of edits for the post.
 */
declare function fetchPostEdits(client: AnyClient, request: PostEditsRequest): ResultAsync<Paginated<PostEdit>, UnexpectedError>;

/**
 * Fetch a SNS subscription.
 *
 * ```ts
 * const result = await fetchSnsSubscription(sessionClient, {
 *   account: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 * });
 * ```
 *
 * @param client - The session client logged as a builder.
 * @param request - The query request.
 * @returns The details of the SNS subscription.
 */
declare function fetchSnsSubscription(client: SessionClient, request: GetSnsSubscriptionsRequest): ResultAsync<Array<SnsSubscription> | [], UnexpectedError>;
/**
 * Create SNS subscriptions.
 *
 * ```ts
 * const result = await createSnsSubscription(sessionClient, {
 *   topics: [
 *     { accountMentioned: evmAddress('0x1234…') },
 *     { accountFollowed: evmAddress('0x90ab…') },
 *   ],
 *   webhook: uri('https://example.com'),
 * });
 * ```
 *
 * @param client - The session client logged as a builder.
 * @param request - The mutation request.
 * @returns List of SNS subscriptions created.
 */
declare function createSnsSubscriptions(client: SessionClient, request: CreateSnsSubscriptionRequest): ResultAsync<Array<SnsSubscription>, UnexpectedError | UnauthenticatedError>;
/**
 * Delete a SNS subscription.
 *
 * ```ts
 * const result = await deleteSnsSubscription(sessionClient, {
 *   id: "1234-dasdf-...",
 * });
 * ```
 *
 * @param client - The session client logged as a builder.
 * @param request - The mutation request.
 * @returns Void
 */
declare function deleteSnsSubscription(client: SessionClient, request: DeleteSnsSubscriptionRequest): ResultAsync<void, UnexpectedError | UnauthenticatedError>;

/**
 * Fetch a Sponsorship.
 *
 * ```ts
 * const result = await fetchSponsorship(anyClient, {
 *   address: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 * });
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The query request.
 * @returns The details of Sponsorship or null if not found.
 */
declare function fetchSponsorship(client: AnyClient, request: SponsorshipRequest): ResultAsync<Sponsorship | null, UnexpectedError>;
/**
 * Fetch paginated Sponsorships.
 *
 * ```ts
 * const result = await fetchSponsorships(anyClient, {
 *   filter: {
 *     managedBy: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 *   }
 * });
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The query request.
 * @returns The paginated list of Sponsorships.
 */
declare function fetchSponsorships(client: AnyClient, request: SponsorshipsRequest): ResultAsync<Paginated<Sponsorship>, UnexpectedError>;
/**
 * Fetch paginated Sponsorship Signers.
 *
 * ```ts
 * const result = await fetchSponsorshipSigners(anyClient, {
 *   filter: {
 *     sponsorship: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 *   }
 * });
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The query request.
 * @returns The paginated list of Sponsorship Signers.
 */
declare function fetchSponsorshipSigners(client: AnyClient, request: SponsorshipSignersRequest): ResultAsync<Paginated<SponsorshipSigner>, UnexpectedError>;
/**
 * Fetch paginated Sponsorship Grants.
 *
 * ```ts
 * const result = await fetchSponsorshipGrants(anyClient, {
 *   filter: {
 *     sponsorship: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 *   }
 * });
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The query request.
 * @returns The paginated list of Sponsorship Signers.
 */
declare function fetchSponsorshipGrants(client: AnyClient, request: SponsorshipGrantsRequest): ResultAsync<Paginated<SponsorshipGrant>, UnexpectedError>;
/**
 * Fetch paginated exclusion list from rate limits of a given Sponsorship.
 *
 * ```ts
 * const result = await fetchSponsorshipLimitExclusions(anyClient, {
 *   filter: {
 *     sponsorship: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 *   }
 * });
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The query request.
 * @returns The paginated list of excluded addresses.
 */
declare function fetchSponsorshipLimitExclusions(client: AnyClient, request: SponsorshipLimitExclusionsRequest): ResultAsync<Paginated<SponsorshipLimitsExempt>, UnexpectedError>;
/**
 * Create a Sponsorship.
 *
 * ```ts
 * const result = await createSponsorship(sessionClient, {
 *   allowLensAccess: true,
 * });
 * ```
 *
 * @param client - The session client logged in as a builder.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function createSponsorship(client: SessionClient, request: CreateSponsorshipRequest): ResultAsync<CreateSponsorshipResult, UnexpectedError | UnauthenticatedError>;
/**
 * Set Sponsorship metadata.
 *
 * ```ts
 * const result = await setSponsorshipMetadata(sessionClient, {
 *   sponsorship: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 *   metadataUri: uri("lens://4f91..."),
 * });
 * ```
 *
 * @param client - The session client logged in as a builder.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function setSponsorshipMetadata(client: SessionClient, request: SetSponsorshipMetadataRequest): ResultAsync<SetSponsorshipMetadataResult, UnexpectedError | UnauthenticatedError>;
/**
 * Update Sponsorship rate limits.
 *
 * ```ts
 * const result = await updateSponsorshipLimits(sessionClient, {
 *   sponsorship: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 *   rateLimits: {
 *     user: {
 *       window: SponsorshipRateLimitWindow.Hour,
 *       limit: 100,
 *     },
 *     global: {
 *       window: SponsorshipRateLimitWindow.Day,
 *       limit: 1_000_000,
 *     },
 *   }
 * });
 * ```
 *
 * Remove one limit by setting it to null or not providing it.
 * ```ts
 * const result = await updateSponsorshipLimits(sessionClient, {
 *   sponsorship: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 *   rateLimits: {
 *     user: null,
 *     global: {
 *       window: SponsorshipRateLimitWindow.Day,
 *       limit: 1_000_000,
 *     },
 *   },
 * });
 * ```
 *
 * Remove all rate limits by setting them to null.
 * ```ts
 * const result = await updateSponsorshipLimits(sessionClient, {
 *   sponsorship: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 *   rateLimits: null,
 * });
 * ```
 *
 * @param client - The session client logged in as a builder.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function updateSponsorshipLimits(client: SessionClient, request: UpdateSponsorshipLimitsRequest): ResultAsync<UpdateSponsorshipLimitsResult, UnexpectedError | UnauthenticatedError>;
/**
 * Update exclusion list from rate limits for a given Sponsorship.
 *
 * ```ts
 * const result = await updateSponsorshipExclusionList(sessionClient, {
 *   sponsorship: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 *   toAdd: [
 *     {
 *       address: evmAddress('0x1234…'),
 *       label: "Bob The Builder",
 *     },
 *   ],
 *   toRemove: [
 *     evmAddress('0x5678…'),
 *   ],
 * });
 * ```
 *
 * @param client - The session client logged in as a builder.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function updateSponsorshipExclusionList(client: SessionClient, request: UpdateSponsorshipExclusionListRequest): ResultAsync<UpdateSponsorshipExclusionListResult, UnexpectedError | UnauthenticatedError>;
/**
 * Update the list of signers for a given Sponsorship.
 *
 * ```ts
 * const result = await updateSponsorshipSigners(sessionClient, {
 *   sponsorship: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 *   toAdd: [
 *     {
 *       address: evmAddress('0x1234…'),
 *       label: "Server A",
 *     },
 *   ],
 *   toRemove: [
 *     evmAddress('0x5678…'),
 *   ],
 * });
 * ```
 *
 * @param client - The session client logged in as a builder.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function updateSponsorshipSigners(client: SessionClient, request: UpdateSponsorshipSignersRequest): ResultAsync<UpdateSponsorshipSignersResult, UnexpectedError | UnauthenticatedError>;
/**
 * Pause sponsorship.
 *
 * ```ts
 * const result = await pauseSponsorship(sessionClient, {
 *   sponsorship: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 * });
 * ```
 *
 * @param client - The session client logged in as a builder.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function pauseSponsorship(client: SessionClient, request: PausingRequest): ResultAsync<PausingResult, UnexpectedError | UnauthenticatedError>;
/**
 * Unpause sponsorship.
 *
 * ```ts
 * const result = await unpauseSponsorship(sessionClient, {
 *   sponsorship: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 * });
 * ```
 *
 * @param client - The session client logged in as a builder.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function unpauseSponsorship(client: SessionClient, request: PausingRequest): ResultAsync<PausingResult, UnexpectedError | UnauthenticatedError>;

/**
 * Fetch timeline from an account.
 *
 * ```ts
 * const result = await fetchTimeline(sessionClient, {
 *   account: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 * });
 * ```
 *
 * @param client - The session client for the authenticated Account.
 * @param request - The query request.
 * @returns The list of timeline items.
 */
declare function fetchTimeline(client: SessionClient, request: TimelineRequest): ResultAsync<Paginated<TimelineItem>, UnexpectedError>;
/**
 * Fetch Timeline Highlights for an account.
 *
 * ```ts
 * const result = await fetchTimelineHighlights(anyClient, {
 *   account: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 * });
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The query request.
 * @returns The list of highlights post for an account.
 */
declare function fetchTimelineHighlights(client: AnyClient, request: TimelineHighlightsRequest): ResultAsync<Paginated<AnyPost>, UnexpectedError>;

/**
 * Fetch the indexing status of a Transaction.
 *
 * ```ts
 * const result = await transactionStatus(anyClient, {
 *   txHash: txHash('0x97589c9e3a3c5b007d…'),
 * });
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The query request.
 * @returns The indexing status of the Transaction.
 */
declare function transactionStatus(client: AnyClient, request: TransactionStatusRequest): ResultAsync<TransactionStatusResult, UnexpectedError>;
/**
 * Prepare a signer ERC20 approval transaction.
 *
 * ```ts
 * const result = await prepareSignerErc20Approval(sessionClient, {
 *   approval: {
 *     infinite: evmAddress('0x1235678901234567890123456789012345678901'),
 *   },
 * });
 * ```
 *
 * @param client - Session Lens client.
 * @param request - The mutation request.
 * @returns The prepared transaction.
 */
declare function prepareSignerErc20Approval(client: SessionClient, request: PrepareSignerErc20ApprovalRequest): ResultAsync<PrepareSignerErc20ApprovalResult, UnauthenticatedError | UnexpectedError>;

/**
 * Transfer primitive ownership.
 *
 * ```ts
 * const result = await transferPrimitiveOwnership(sessionClient, {
 *   newOwner: evmAddress('0x1234…'),
 *   address: evmAddress('0x5678…'),
 * });
 * ```
 *
 * @param client - Session client.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function transferPrimitiveOwnership(client: SessionClient, request: TransferPrimitiveOwnershipRequest): ResultAsync<TransferPrimitiveOwnershipResult, UnauthenticatedError | UnexpectedError>;

/**
 * Checks if the given username can be created by the logged in Account.
 *
 * ```ts
 * const result = await canCreateUsername(sessionClient, {
 *   localName: 'wagmi',
 * });
 * ```
 */
declare function canCreateUsername(client: SessionClient, request: CanCreateUsernameRequest): ResultAsync<CanCreateUsernameResult, UnexpectedError | UnauthenticatedError>;
/**
 * Create a username
 *
 * ```ts
 * const result = await createUsername(sessionClient, {
 *   username: {
 *     localName: 'wagmi',
 *   },
 * });
 * ```
 *
 * @param client - The session client for the authenticated Account.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function createUsername(client: SessionClient, request: CreateUsernameRequest): ResultAsync<CreateUsernameResult, UnexpectedError | UnauthenticatedError>;
/**
 * Assign a username to the account associated with the authenticated session.
 *
 * ```ts
 * const result = await assignUsernameToAccount(sessionClient, {
 *   username: {
 *     localName: 'wagmi',
 *   },
 * });
 * ```
 *
 * @param client - The session client for the authenticated Account.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function assignUsernameToAccount(client: SessionClient, request: AssignUsernameToAccountRequest): ResultAsync<AssignUsernameToAccountResult, UnauthenticatedError | UnexpectedError>;
/**
 * Unassign a username to the account associated with the authenticated session.
 *
 * ```ts
 * const result = await unassignUsernameFromAccount(sessionClient, {
 *   username: {
 *     localName: 'wagmi',
 *   },
 * });
 * ```
 *
 * @param client - The session client for the authenticated Account.
 * @param request - The mutation request.
 * @returns Tiered transaction result.
 */
declare function unassignUsernameFromAccount(client: SessionClient, request?: UnassignUsernameFromAccountRequest): ResultAsync<UnassignUsernameToAccountResult, UnauthenticatedError | UnexpectedError>;
/**
 * Fetch username details.
 *
 * ```ts
 * const result = await fetchUsername(anyClient, {
 *   username: {
 *     localName: 'wagmi',
 *   },
 * });
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The query request.
 * @returns The username details.
 */
declare function fetchUsername(client: AnyClient, request: UsernameRequest): ResultAsync<Username | null, UnexpectedError>;
/**
 * Fetch usernames.
 * Example: fetch usernames owned by a specific address.
 *
 * ```ts
 * const result = await fetchUsernames(anyClient, {
 *   filter: {
 *     owner: evmAddress('0xe2f2a5C287993345a840db3B0845fbc70f5935a5'),
 *   },
 * });
 * ```
 *
 * @param client - Any Lens client.
 * @param request - The query request.
 * @returns The list of usernames.
 */
declare function fetchUsernames(client: AnyClient, request: UsernamesRequest): ResultAsync<Paginated<Username>, UnexpectedError>;

export { addAccountManager, addAdmins, addAppAuthorizationEndpoint, addAppFeeds, addAppGroups, addAppSigners, addPostNotInterested, addReaction, approveGroupMembershipRequests, assignUsernameToAccount, banGroupAccounts, blockAccount, bookmarkPost, canCreateUsername, cancelGroupMembershipRequest, configureAccountAction, configurePostAction, createAccount, createAccountWithUsername, createApp, createFeed, createFrameTypedData, createGraph, createGroup, createSnsSubscriptions, createSponsorship, createUsername, createUsernameNamespace, currentSession, deletePost, deleteSnsSubscription, deposit, disableAccountAction, disablePostAction, dismissRecommendedAccounts, editPost, enableAccountAction, enablePostAction, enableSignless, executeAccountAction, executePostAction, fetchAccessControl, fetchAccount, fetchAccountBalances, fetchAccountFeedStats, fetchAccountGraphStats, fetchAccountManagers, fetchAccountRecommendations, fetchAccountStats, fetchAccounts, fetchAccountsAvailable, fetchAccountsBlocked, fetchAccountsBulk, fetchAdminsFor, fetchApp, fetchAppFeeds, fetchAppGroups, fetchAppServerAPiKey, fetchAppSigners, fetchAppUsers, fetchApps, fetchAuthenticatedSessions, fetchBalancesBulk, fetchFeed, fetchFeeds, fetchFollowStatus, fetchFollowers, fetchFollowersYouKnow, fetchFollowing, fetchGraph, fetchGraphs, fetchGroup, fetchGroupBannedAccounts, fetchGroupMembers, fetchGroupMembershipRequests, fetchGroupStats, fetchGroups, fetchMeDetails, fetchNamespace, fetchNamespaceReservedUsernames, fetchNamespaces, fetchNotifications, fetchPost, fetchPostActionContracts, fetchPostBookmarks, fetchPostEdits, fetchPostReactionStatus, fetchPostReactions, fetchPostReferences, fetchPostTags, fetchPosts, fetchPostsForYou, fetchPostsToExplore, fetchSnsSubscription, fetchSponsorship, fetchSponsorshipGrants, fetchSponsorshipLimitExclusions, fetchSponsorshipSigners, fetchSponsorships, fetchTimeline, fetchTimelineHighlights, fetchTokenDistributions, fetchUsername, fetchUsernames, fetchWhoExecutedActionOnAccount, fetchWhoExecutedActionOnPost, fetchWhoReferencedPost, findErc20Amount, findNativeAmount, follow, generateNewAppServerApiKey, health, hideManagedAccount, hideReply, joinGroup, lastLoggedInAccount, leaveGroup, legacyRolloverRefresh, muteAccount, pauseSponsorship, post, prepareSignerErc20Approval, recommendAccount, refresh, refreshMetadata, refreshMetadataStatus, rejectGroupMembershipRequests, removeAccountManager, removeAdmins, removeAppAuthorizationEndpoint, removeAppFeeds, removeAppGroups, removeAppSigners, removeGroupMembers, removeSignless, reportAccount, reportPost, repost, requestGroupMembership, revokeAuthentication, setAccountMetadata, setAppGraph, setAppMetadata, setAppSponsorship, setAppTreasury, setAppUsernameNamespace, setAppVerification, setDefaultAppFeed, setFeedMetadata, setGraphMetadata, setGroupMetadata, setNamespaceMetadata, setSponsorshipMetadata, signFrameAction, switchAccount, transactionStatus, transferPrimitiveOwnership, unassignUsernameFromAccount, unbanGroupAccounts, unblockAccount, undoBookmarkPost, undoPostNotInterested, undoReaction, undoRecommendAccount, unfollow, unhideManagedAccount, unhideReply, unmuteAccount, unpauseSponsorship, unwrapTokens, updateAccountFollowRules, updateAccountManager, updateFeedRules, updateGraphRules, updateGroupRules, updateNamespaceRules, updatePostRules, updateReservedUsernames, updateSponsorshipExclusionList, updateSponsorshipLimits, updateSponsorshipSigners, verifyFrameSignature, waitForMetadata, withdraw, wrapTokens };
