import { Signer, TransactionRequest } from 'ethers';
import { o as OperationHandler, b as SignMessage } from '../clients-DlizIY8p.js';
import { Wallet } from '@lens-chain/sdk/ethers';
import { EvmAddress } from '@lens-protocol/types';
import { types } from 'zksync-ethers';
import '@lens-protocol/graphql';
import '@lens-protocol/storage';
import '@urql/core';
import '@lens-protocol/env';

/**
 * Handles a transaction mutation result.
 *
 * In case the result is a transaction request, it will be signed and sent using the provided signer.
 */
declare function handleOperationWith(signer: Signer): OperationHandler;
/**
 * @deprecated Use {@link handleOperationWith} instead.
 */
declare const handleWith: typeof handleOperationWith;
/**
 * Sign an Ethereum message with the provided signer.
 */
declare function signMessageWith(signer: Signer): SignMessage;

/**
 * The configuration for the `SponsorshipApprovalSigner`.
 *
 * @experimental This is an experimental helper and may be subject to breaking changes.
 */
type SponsorshipApprovalSignerConfig = {
    /**
     * The ethers.js signer to use for signing the sponsorship approval.
     */
    signer: Wallet;
    /**
     * The address of the Sponsorship contract.
     */
    sponsorship: EvmAddress;
    /**
     * The validity period for the sponsorship approval in seconds.
     *
     * @default 5 minutes
     */
    validity?: number;
};
/**
 * An helper class to sign sponsorship approvals for arbitrary transactions using ethers.js.
 *
 * @experimental This is an experimental helper and may be subject to breaking changes.
 */
declare class SponsorshipApprovalSigner {
    private readonly context;
    constructor(config: SponsorshipApprovalSignerConfig);
    approveSponsorship(request: TransactionRequest): Promise<types.TransactionLike>;
    private computeDeadline;
    private fillCustomData;
    private encodePaymasterInput;
}

export { SponsorshipApprovalSigner, type SponsorshipApprovalSignerConfig, handleOperationWith, handleWith, signMessageWith };
